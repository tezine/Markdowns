import { DisplayUnlessDirective } from './display-unless.directive';

describe('DisplayUnlessDirective', () => {
  it('should create an instance', () => {
    const directive = new DisplayUnlessDirective();
    expect(directive).toBeTruthy();
  });
});
